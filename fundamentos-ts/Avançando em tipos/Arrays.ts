let numbers: number[] = [1, 2, 3]; // Apenas números podem ser inseridos neste array
let nomes: string[] = ['Halister', 'Priscila']; // Apenas strings podem ser inseridas neste array

// Outra sintaxe para arrays

const nums: Array<number> = [4, 5, 6]