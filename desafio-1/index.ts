// Crie uma função que aceita dois argumentos, somente do tipo número e exibe a soma entre os dois

const x = 10;
const y = 20;

function sumNumbers(num1: number, num2: number) {
    console.log(num1 + num2);
}

sumNumbers(x, y);
