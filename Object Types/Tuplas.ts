// Um tipo de array que definimos a quantidade e o tipo de elementos
// Criamos um novo type e nele inserimos um array com os tipos necessários
// Cada tipo conta como um elemento

type fiveNumbers = [number, number, number, number, number]

const myNumArr: fiveNumbers = [ 1, 2, 3, 4, 5]

type nameAndAge = [string, number];

const anotherUser: nameAndAge = [ 'halister', 32];